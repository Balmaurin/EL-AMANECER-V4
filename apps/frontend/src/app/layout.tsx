import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Sheily MCP - Enterprise AI Platform',
  description: 'Multi-Agent AI Orchestration Platform for Enterprise Applications',
  keywords: ['AI', 'Multi-Agent', 'Enterprise', 'Orchestration', 'Dashboard', 'Automation'],
  authors: [{ name: 'Sheily MCP Team' }],
  robots: 'noindex, nofollow', // For enterprise deployment
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="h-full">
      <body className={`${inter.className} h-full bg-gray-50`}>
        <Providers>
          {children}
        </Providers>
        <Toaster
          position="top-right"
          toastOptions={{
            duration: 4000,
            style: {
              background: '#363636',
              color: '#fff',
            },
            success: {
              duration: 3000,
            },
            error: {
              duration: 5000,
            },
          }}
        />
      </body>
    </html>
  )
}
