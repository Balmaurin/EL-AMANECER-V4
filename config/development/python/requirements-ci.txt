# Core dependencies for Sheily AI - CI/CD optimized
# Minimal versions for GitHub Actions - no strict pins to avoid conflicts

# Web Framework
# FastAPI and Starlette must be compatible
# FastAPI 0.121.x requires starlette>=0.40.0,<0.50.0
fastapi>=0.120.0,<0.122.0
starlette>=0.43.0,<0.50.0
uvicorn[standard]>=0.30.0
websockets>=14.0

# Data Science & ML - flexible versions
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Only essential ML packages - skip heavy ones for CI
# torch and transformers omitted to speed up CI
# Install only if needed by specific tests

# Vector Database (lightweight)
# chromadb omitted - too heavy for CI
# faiss-cpu omitted - install only if needed

# Database
psycopg2-binary>=2.9.0
asyncpg>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.13.0

# Cache
redis>=5.0.0

# Monitoring
prometheus-client>=0.20.0
psutil>=5.9.0

# Logging
python-json-logger>=3.0.0
structlog>=24.0.0

# Security
cryptography>=44.0.0
bcrypt>=4.2.0
pyjwt[crypto]>=2.10.0

# HTTP Client
aiohttp>=3.13.0
httpx>=0.28.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.12.0

# Utilities
click>=8.1.0
rich>=13.0.0
tqdm>=4.67.0

# Development & Testing - essential only
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
black>=24.0.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.13.0
bandit>=1.8.0

