# Sheily AI - Configuración de Entorno SEGURA
# =================================================
#
# Variables de entorno para el sistema completo de entrenamiento automático LoRA
# ✅ CONFIGURACIÓN SEGURA - NO USAR VALORES POR DEFECTO EN PRODUCCIÓN

# === CONFIGURACIÓN GENERAL ===
ENVIRONMENT=production
PYTHONPATH=/app
PYTHONUNBUFFERED=1

# === BACKEND CONFIG ===
HOST=0.0.0.0
PORT=8006
DASHBOARD_PORT=8000
LOG_LEVEL=INFO

# === ENTRENAMIENTO LoRA ===
LORA_MODEL_NAME=microsoft/DialoGPT-medium
LORA_EPOCHS=3
LORA_BATCH_SIZE=4
LORA_LEARNING_RATE=0.0002
LORA_R=16
LORA_ALPHA=32
LORA_DROPOUT=0.1

# === SISTEMA RAG ===
RAG_UPDATE_INTERVAL=300
RAG_SIMILARITY_THRESHOLD=0.7
RAG_MAX_RESULTS=5

# === BASE DE DATOS ===
DB_PATH=/app/data/sheily_dashboard.db

# === DIRECTORIOS DE DATOS ===
DATA_DIR=/app/data
MODELS_DIR=/app/data/trained_models
DATASETS_DIR=/app/data/datasets
UPLOADS_DIR=/app/data/uploads
TEMP_DIR=/app/data/temp_training
LOGS_DIR=/app/logs

# === CONFIGURACIÓN DE SEGURIDAD - CRÍTICA ===
# ❌ NUNCA usar valores por defecto en producción
# Generar claves seguras usando: openssl rand -hex 32
SECRET_KEY=CHANGE_THIS_IN_PRODUCTION_GENERATE_SECURE_RANDOM_KEY
CORS_ORIGINS=https://your-domain.com,https://app.your-domain.com

# === CONFIGURACIÓN DE AUTENTICACIÓN ===
AUTH_ENABLED=true
JWT_SECRET_KEY=CHANGE_THIS_IN_PRODUCTION_GENERATE_SECURE_JWT_KEY
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# === CONFIGURACIÓN DE API KEYS - SEGURAS ===
# Usar Vault o AWS Secrets Manager en producción
OPENAI_API_KEY=your-secure-openai-api-key-here
ANTHROPIC_API_KEY=your-secure-anthropic-api-key-here

# === CONFIGURACIÓN DE BASE DE DATOS SEGURA ===
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sheily_prod
DB_USER=sheily_app
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_IN_PRODUCTION
DB_SSL_MODE=require

# === CONFIGURACIÓN DE REDIS SEGURA ===
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_THIS_STRONG_REDIS_PASSWORD
REDIS_SSL=true

# === CONFIGURACIÓN DE VAULT (HashiCorp) ===
VAULT_ENABLED=true
VAULT_ADDR=https://vault.your-domain.com:8200
VAULT_TOKEN=CHANGE_THIS_VAULT_TOKEN
VAULT_ROLE=sheily-ai-api

# === CONFIGURACIÓN DE BACKUP (OPCIONAL) ===
BACKUP_INTERVAL=3600
BACKUP_RETENTION=7
BACKUP_ENCRYPTION_KEY=CHANGE_THIS_BACKUP_ENCRYPTION_KEY

# === CONFIGURACIÓN DE MONITOREO (OPCIONAL) ===
PROMETHEUS_PORT=9090
METRICS_INTERVAL=30
GRAFANA_ADMIN_PASSWORD=CHANGE_THIS_GRAFANA_ADMIN_PASSWORD

# === LIMITES Y CUOTAS ===
MAX_FILE_SIZE=10485760  # 10MB
MAX_FILES_PER_UPLOAD=10
MAX_DATASETS_PER_DAY=50
MAX_TRAINING_JOBS=3

# === CONFIGURACIÓN DE RATE LIMITING ===
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST=10

# === CONFIGURACIÓN DE LOGGING SEGURA ===
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_MAX_FILE_SIZE=10485760  # 10MB
LOG_BACKUP_COUNT=5
LOG_SENSITIVE_FIELDS=password,secret,key,token,api_key

# === CONFIGURACIÓN DE AUDITORÍA ===
AUDIT_ENABLED=true
AUDIT_LOG_PATH=/app/logs/audit.log
AUDIT_RETENTION_DAYS=90

# === CONFIGURACIÓN DE ENCRIPTACIÓN ===
ENCRYPTION_ENABLED=true
ENCRYPTION_ALGORITHM=AES256
ENCRYPTION_KEY_ROTATION_DAYS=30

# === CONFIGURACIÓN DE COMPLIANCE ===
COMPLIANCE_GDPR_ENABLED=true
COMPLIANCE_CCPA_ENABLED=true
DATA_RETENTION_DAYS=2555  # 7 años para GDPR

# === CONFIGURACIÓN DE ALERTAS ===
ALERT_EMAIL_ENABLED=true
ALERT_EMAIL_RECIPIENTS=security@your-domain.com,admin@your-domain.com
ALERT_SLACK_WEBHOOK=CHANGE_THIS_SLACK_WEBHOOK_URL

# === CONFIGURACIÓN DE MANTENIMIENTO ===
MAINTENANCE_MODE=false
MAINTENANCE_MESSAGE="Sistema en mantenimiento programado"

# === CONFIGURACIÓN DE FUNCIONES EXPERIMENTALES ===
EXPERIMENTAL_FEATURES_ENABLED=false
BETA_TESTING_ENABLED=false

# =================================================================
# 🚨 INSTRUCCIONES DE SEGURIDAD CRÍTICAS:
#
# 1. NUNCA commitear este archivo con valores reales a Git
# 2. Usar variables de entorno del sistema para producción
# 3. Rotar todas las claves cada 30 días
# 4. Usar HashiCorp Vault o AWS Secrets Manager
# 5. Implementar principle of least privilege
# 6. Monitorear acceso a secrets continuamente
#
# Comando para generar clave segura:
# python -c "import secrets; print(secrets.token_hex(32))"
#
# Comando para validar configuración:
# python -m sheily_core.mcp_server --validate-config
# =================================================================
